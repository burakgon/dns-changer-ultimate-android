# Deploy Build to Telegram

Run the build and deploy script to:
1. Build the debug APK
2. Generate HTML changelog with commits since last build
3. Send APK and changelog to Telegram

Execute this command:

```bash
./build-and-send.sh
```

After running, confirm the build was sent successfully and report the version number and commit count.
